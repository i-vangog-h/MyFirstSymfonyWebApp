{% extends 'base.html.twig' %}
{% block title %} Create a new Record | {{parent()}} {% endblock %}
{% block body %}

    <h1>{{ title }}</h1> {# {{}} is a "say somethig" syntax #}

    {# TODO: Add an image of the record #}

    <div>   
        Tracks: 

        <ul> 
            {% for track in tracks %} {# "Do something" syntax #}
                <li>{{track.song}} - {{track.artist|upper }}</li>
            {% endfor %}
        </ul>

    </div>

{% endblock %}
